# rdm-assets README

Watches any folder named 'assets' in the MSD-LIVE RDM dev container workspace and when a new file is created this extension will run `invenio-cli assets build` in order to have the new file symlinkd and watched for changes that will trigger webpack rebuilds

